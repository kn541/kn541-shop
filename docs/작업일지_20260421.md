# KN541 쇼핑몰 작업일지

**작업일**: 2026년 4월 21일  
**세션**: Claude 협업 세션 3

---

## 1. 이번 세션 완료 작업

### ✅ fly.dev → Railway 전환 완료
- `admin/starter-kit/.env.local` (로컬): fly.dev → railway URL 변경
- `kn541-scm/src/lib/scmApi.ts` fallback URL: fly.dev → railway 변경 후 GitHub push
- Vercel 3개 프로젝트(admin/shop/scm) 모두 Railway URL로 확인

### ✅ 이미지 업로드 RLS 에러 수정
- **원인**: `database.py`가 `SUPABASE_ANON_KEY`로 Storage 클라이언트 생성 → RLS 차단(403)
- **수정**: `backend/routers/upload.py` — `SUPABASE_SERVICE_ROLE_KEY` 우선 사용
- Railway에 `SUPABASE_SERVICE_ROLE_KEY` 등록

### ✅ SSG 타임아웃 근본 해결
- `[locale]/layout.tsx` — `force-dynamic` 전역 추가 + `generateStaticParams` 제거
- `coming-soon/layout.tsx` — `force-dynamic` 추가

### ✅ 어드민 상품 이미지 클릭 → 쇼핑몰 상세페이지 연결
- **원인**: 백엔드 keyword 검색(PGroonga)이 `product_code` 숫자값 매칭 안 됨
- **수정 1**: `kn541-shop/lib/api/products.ts` — `getProductByCode`에 UUID 직접 조회 + keyword 없이 전체 목록 조회 후 product_code 정확 매칭
- **수정 2**: 어드민 `ProductListTable.tsx` — 이미지 클릭 URL을 `product_id(UUID)` 기반으로 변경
- **결과**: 어드민 이미지 클릭 → `{SHOP_URL}/ko/products/{UUID}` → 쇼핑몰 직접 조회 ✅

### ✅ 쇼핑몰 상품 상세페이지 전면 개선
- **문제 1** (썸네일 안 보임): `next.config.mjs`에 `dbimg.co.kr` 등 외부 도메인 없음
  - `remotePatterns` → 모든 https/http 도메인 허용으로 변경
  - `KoreanProductGallery.tsx` — `next/image` → `<img>` 태그로 변경
- **문제 2** (카테고리 브레드크럼): 하드코딩 상태
  - `adapters.ts` — `categoryBreadcrumbs`, `categoryName1`, `categoryName2` 등 추가
  - `[handle]/page.tsx` — 실제 카테고리 브레드크럼 연결
- **문제 3** (상품 정보 누락): 원산지, 과세유형, 재고, 공급사, 상품코드 등 누락
  - `adapters.ts` — 전체 필드 매핑 추가 (`origin`, `taxLabel`, `stockQty`, `supplierName` 등)
  - `[handle]/page.tsx` — 상품 정보표 추가

### ✅ 카테고리 필터 백엔드 수정
- **원인**: `category_id` 소분류로만 매칭 → 대분류/중분류 선택 시 0개
  - `category_id_1`(대분류)로 681개, `category_id_2`(중분류)로 6개, `category_id`(소분류)로 0개
- **수정**: `backend/products.py` — `category_id` 필터를 OR 조건으로 변경
  ```python
  query.or_("category_id.eq.{cid},category_id_1.eq.{cid},category_id_2.eq.{cid}")
  ```

---

## 2. 미완료 / 현재 진행 중 이슈

### 🔴 [CRITICAL] 쇼핑몰 상품 목록 페이지 상품 0개 표시

**URL**: `https://shop.kn541.co.kr/ko/products`

**증상**: 전체 상품 및 카테고리 선택 시 모두 「상품이 없습니다.」 표시

**분석 결과**:
| 항목 | 결과 |
|------|------|
| API 직접 호출 (`/products?size=5`) | ✅ 정상 (3-5개 반환) |
| 카테고리 OR 조건 (`category_id_1=UUID`) | ✅ 정상 (681개 반환) |
| Vercel 런타임 로그 | 200 OK (에러 없음) |
| `page.tsx` 코드 확인 | `product_status: 'ACTIVE'` 필터 이미 제거됨 |
| 최신 배포 | `dpl_EFLFt513r96EpqAtk3paXQDL9QhC` READY |

**의심 원인 (미확인)**:
1. Vercel 빌드 캐시 문제 → `NEXT_PUBLIC_API_URL`이 서버 컴포넌트에서 `undefined`일 가능성
2. `page.tsx`에서 `searchParams`를 await할 때 Next.js 캐싱 동작 이상
3. 현재 배포된 Vercel에 `page.tsx` 이전 버전이 적용됐을 가능성

**다음 세션 디버깅 방법**:
```
1. shop 페이지에서 네트워크 탭 확인
   → /ko/products 페이지 자체에서 백엔드 API 호출하는지 확인
2. Vercel 환경변수 확인
   → NEXT_PUBLIC_API_URL = https://kn541-production.up.railway.app 인지 확인
3. page.tsx에 디버그 로그 추가 후 Vercel 런타임 로그 확인
   → console.log('[DEBUG] BASE:', BASE, 'categoryId:', cid)
4. 또는 page.tsx를 완전히 Client Component로 변경해서 브라우저에서 직접 API 호출
```

---

## 3. 현재 인프라 현황

| 서비스 | URL | 상태 |
|--------|-----|------|
| 쇼핑몰 (shop) | shop.kn541.co.kr | ✅ 배포 중 (Vercel: prj_79A3AewCKMp1kdLiPIMVp82yv3Ik) |
| 어드민 (admin) | admin.kn541.co.kr | ✅ 배포 중 (Vercel: prj_sZ5L9j9Ng7jrWvZz2FPdo9YRPFE9) |
| SCM | scm.kn541.co.kr | ✅ 배포 중 |
| 백엔드 | kn541-production.up.railway.app | ✅ Railway ACTIVE |
| DB | qxmcbdqmmiyrrhenufaj.supabase.co | ✅ 정상 (Seoul 리전) |

---

## 4. 코드 변경 파일 목록 (이번 세션)

| 레포 | 파일 | 변경 내용 |
|------|------|----------|
| kn541 (백엔드) | `backend/routers/products.py` | category_id OR 조건, product_code 파라미터 추가, size max 200 |
| kn541 (백엔드) | `backend/routers/upload.py` | SUPABASE_SERVICE_ROLE_KEY 우선 사용 |
| kn541 (어드민) | `admin/starter-kit/src/views/products/ProductListTable.tsx` | 이미지 클릭 URL → UUID 기반 |
| kn541-shop | `ciseco-nextjs/next.config.mjs` | remotePatterns 모든 도메인 허용 |
| kn541-shop | `ciseco-nextjs/src/lib/api/products.ts` | getProductByCode UUID 지원 + 전체 목록 필터 |
| kn541-shop | `ciseco-nextjs/src/lib/adapters.ts` | 카테고리 브레드크럼, 원산지, 과세유형 등 전체 필드 |
| kn541-shop | `ciseco-nextjs/src/app/[locale]/(shop)/(other-pages)/products/KoreanProductGallery.tsx` | img 태그로 변경 |
| kn541-shop | `ciseco-nextjs/src/app/[locale]/(shop)/(other-pages)/products/[handle]/page.tsx` | 상세페이지 전면 개선 |
| kn541-shop | `ciseco-nextjs/src/app/[locale]/(shop)/(other-pages)/products/page.tsx` | product_status 필터 제거, UUID handle |

---

*작성: Claude (기획 창) | 2026-04-21*
